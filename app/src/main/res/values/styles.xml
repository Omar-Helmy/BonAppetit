<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brownPrimary</item>
        <item name="colorPrimaryDark">@color/brownPrimary</item>
        <item name="colorAccent">@color/brownAccent</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
        <!--the following overlap when true, will start animation of entering while exist not finished yet
        but when false, will wait for exit first then enter starts-->
        <item name="android:windowAllowEnterTransitionOverlap">false</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowExitTransition">@transition/exit_transition</item>
        <item name="android:windowEnterTransition">@transition/enter_transition</item>
        <item name="android:windowReturnTransition">@transition/return_transition</item>
        <item name="android:windowReenterTransition">@transition/reenter_transition</item>

        <!-- specify shared element transitions -->
        <!--<item name="android:windowSharedElementEnterTransition">-->
        <!--@transition/shared_element_transition</item>-->
        <!--<item name="android:windowSharedElementExitTransition">-->
        <!--@transition/shared_element_transition</item>-->

    </style>

    <style name="AppTheme.Launch">
        <item name="android:windowBackground">@drawable/launch_screen</item>

    </style>

    <style name="AppTheme.Transition">

    </style>


</resources>
